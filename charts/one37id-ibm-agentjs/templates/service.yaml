apiVersion: v1
kind: Service
metadata:
  name: {{ include "one37id-ibm-agentjs.fullname" . }}
  labels:
    {{- include "one37id-ibm-agentjs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range $key, $value := .Values.service.ports }}
    - port: {{ $value }}
      targetPort: {{ $value }}
      protocol: TCP
      name: {{ $key}}
    {{- end }}
  selector:
    {{- include "one37id-ibm-agentjs.selectorLabels" . | nindent 4 }}
