apiVersion: v1
kind: Service
metadata:
  name: {{ include "one37id-mediator.fullname" . }}
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "one37id-mediator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "one37id-mediator.selectorLabels" . | nindent 4 }}
