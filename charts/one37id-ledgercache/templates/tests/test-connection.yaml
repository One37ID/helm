apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "one37id-ledgercache.fullname" . }}-test-connection"
  labels:
    {{- include "one37id-ledgercache.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "one37id-ledgercache.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
